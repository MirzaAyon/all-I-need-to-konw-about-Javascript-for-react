Which one is true about map and forEach when called on an empty array ?
//map returns array but foreach returns undefined

const adventurer = { name: 'Alice', cat: { name: 'Lucy' } };
const ratName = adventurer?.rat?.name;
//undefined ans

let person = null;
console.log(person ? person : "person is null");
What will be the output of the above code ?
    //person is null  - ans


    Which one is not true for the filter(array method filter) ?
//bhul returns undefined when no elements pass the test - false
//bhul returns empty array when no elements pass the test - true


const obj = { a: 1 };
console.log(Object.keys(obj).length === 0);
What will be the output ?
    //false ans


    which one is false
//we can check length of string and array
//reducer is an array method
//{} "empty object" is falsy value